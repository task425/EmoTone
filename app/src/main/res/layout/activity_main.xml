<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/title"
    android:orientation="vertical"
    android:theme="@style/Theme.styles"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:layout_gravity="bottom"
            android:orientation="vertical"
            android:padding="0dp">

            <Button
                android:elevation="4dp"
                android:id="@+id/keisoku_button"
                android:layout_width="match_parent"
                android:layout_height="88dp"
                android:layout_gravity="center"
                android:layout_marginHorizontal="25dp"
                android:background="@drawable/setumei_button_bg"
                android:fontFamily="@font/logo"
                android:text="計測スタート"
                android:textColor="@color/white"
                android:textSize="24dp"/>


            <Button
                android:textColor="#333"
                android:fontFamily="@font/logo"
                android:id="@+id/setumei_button"
                android:layout_width="200dp"
                android:layout_height="44dp"
                android:layout_gravity="center"
                android:layout_margin="10dp"
                android:background="@drawable/keisoku_button_bg"
                android:textSize="16dp"
                android:elevation="4dp"
                android:text="説明" />
        </LinearLayout>
    </LinearLayout>


</LinearLayout>